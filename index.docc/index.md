# deCLAW (Provisioned) — Scheduling/Cron Detachment Spec

This is a **provisioned DocC bundle** in `todo3` intended to be shareable with other operators/teams.

## Purpose
Define the concrete steps and validation probes required to remove OpenClaw from the cron/scheduling path, replacing it with a CLIA-owned mechanism.

## Entry points
- Source request bundle: `.clia/requests/declaw.docc/`
- Epic: `Epic 1 — Provisioned DocC bundle (remove OpenClaw from cron/scheduling)`

## Status
Draft.

